package controllers;

import actors.Actor;

public class NetworkController extends Controller {
  
  public NetworkController(Actor actor) {
    super(actor);
  }

  @Override
  public void tick(Actor[] actors) {
      
  }
}
